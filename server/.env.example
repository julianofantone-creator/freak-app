# ==============================================
# FREAK VIDEO CHAT SERVER - ENVIRONMENT CONFIG
# ==============================================

# Server Configuration
PORT=8080
NODE_ENV=development

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/freak
# For production, use MongoDB Atlas or similar:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/freak

# Redis Configuration (Optional - for production scaling)
# REDIS_URL=redis://localhost:6379
# For production, use Redis Cloud or similar:
# REDIS_URL=rediss://username:password@hostname:port

# Authentication & Security
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters-long
JWT_EXPIRE=7d
BCRYPT_ROUNDS=12

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
SOCKET_RATE_LIMIT_POINTS=50
SOCKET_RATE_LIMIT_DURATION=60

# WebRTC Configuration
# STUN servers are free, TURN servers require setup
# Example TURN server (you'll need to set up your own):
# TURN_SERVER_URL=turn:your-turn-server.com:3478
# TURN_SERVER_USERNAME=your-username
# TURN_SERVER_CREDENTIAL=your-password

# For production, consider using services like:
# - Twilio STUN/TURN: https://www.twilio.com/stun-turn
# - Xirsys: https://xirsys.com/
# - Metered.ca: https://www.metered.ca/tools/openrelay/

# Matching Algorithm Configuration
MAX_QUEUE_TIME=300000
INTEREST_WEIGHT=0.7
DISTANCE_WEIGHT=0.3
MAX_DISTANCE_KM=50

# Logging Configuration
LOG_LEVEL=info

# Admin Configuration (Optional)
# ADMIN_IPS=127.0.0.1,10.0.0.1
# ADMIN_EMAIL=admin@example.com

# ==============================================
# PRODUCTION DEPLOYMENT NOTES
# ==============================================
#
# 1. Generate a strong JWT secret:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# 2. Set up MongoDB Atlas or similar cloud database
#
# 3. Set up Redis for production scaling (optional but recommended)
#
# 4. Configure TURN servers for reliable WebRTC connections
#
# 5. Set NODE_ENV=production
#
# 6. Use a reverse proxy like Nginx for SSL termination
#
# 7. Set up monitoring and logging (e.g., PM2, Winston, etc.)
#
# ==============================================